/* ============================================================
   連線設定檔 — 只放「技術連線」資訊，不放營運設定。
   店名、服務費、付款方式、營業時間、用餐時間等，
   一律在「後台 → 店家設定」中管理 (存於資料庫，可隨時修改)。

   填入 Supabase 金鑰 → 進入「線上模式」(跨裝置同步)
   留空     → 「示範模式」(瀏覽器本機儲存，可先試用)
   ============================================================ */
window.MIKU_POS_CONFIG = {
  SUPABASE_URL: "",      // 例: https://xxxxxxxx.supabase.co
  SUPABASE_ANON_KEY: "", // 例: eyJhbGciOi...
  CURRENCY: "NT$"        // 幣別符號
};
